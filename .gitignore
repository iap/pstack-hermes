# Python
__pycache__/
*.pyc

# uv-managed virtualenv (uv run creates it; never commit)
.venv/

# validate.py runtime scratch (GOLD+ loader data_root; empty/regenerable)
.validate-data/

# convert.py atomic-build leftovers (never present after a clean run)
pstack.tmp-build/
pstack.prev-build/
